exponent = lambda x, y = 1 : x ** y
''' Returns x^y

    Parameters x and y can be any integer (positive or negative). If parameter y
    is not specified, the default is 1. Returns parameter x to the power of y (x^y).

    >>> exponent(4, 2)
    16
    >>> exponent(6, 5)
    7776
    >>> exponent(4, 1)
    4
    >>> exponent(4)
'''
